{
  "installCommand": "pip install flask --break-system-packages",
  "buildCommand": "echo Build complete",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api.py" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}